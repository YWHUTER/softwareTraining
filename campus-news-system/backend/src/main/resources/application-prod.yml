# 生产环境配置
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campus_news_system?useUnicode=true&characterEncoding=utf8&useSSL=true&serverTimezone=Asia/Shanghai
    username: root
    password: your_password_here
  
  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.campus.news: info
  file:
    name: logs/campus-news.log
